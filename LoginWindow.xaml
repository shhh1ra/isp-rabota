<Window x:Class="AutoSalon.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="220" Width="330"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин"/>
        <TextBox x:Name="LoginBox" Grid.Row="1" Margin="0,6,0,12"/>

        <TextBlock Text="Пароль" Grid.Row="2"/>
        <PasswordBox x:Name="PassBox" Grid.Row="3" Margin="0,6,0,0" Height="28"/>

        <Button Content="Войти" Width="90" Height="30"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="Login_Click"/>
    </Grid>
</Window>
