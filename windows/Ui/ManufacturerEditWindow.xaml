<Window x:Class="AutoSalon.Desktop.Ui.ManufacturerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Производитель" Height="260" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название:" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Страна:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="CountryBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Сайт:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="WebsiteBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="90" Margin="0,0,8,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="90" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
